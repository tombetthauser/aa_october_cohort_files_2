<h3>Links Index</h3>

<a href="<%= new_link_url %>">New Link</a>

<ul>

    <% @links.each do |link| %>
        <li><a href="<%= link_url(link) %>"><%= link.title %></a></li>
        <li><%= link.url %></li>
    <% end %>

</ul>